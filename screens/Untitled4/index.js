import { Text } from "react-native";
import { StyleSheet, SafeAreaView } from "react-native";

const Untitled4 = () => {
  return <SafeAreaView style={styles.safeArea}>
      <Text style={styles.KCIhOOGH}>James Logan Mueller</Text>
    </SafeAreaView>;
};

const styles = StyleSheet.create({
  safeArea: {
    height: "100%"
  },
  KCIhOOGH: {
    width: 355,
    height: 75,
    lineHeight: 77,
    fontSize: 25,
    borderRadius: 0,
    textAlign: "center",
    position: "absolute",
    left: 1
  }
});
export default Untitled4;